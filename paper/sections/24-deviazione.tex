% =====================================================================
% §2.4 — La deviazione per grano: ampiezza e probabilità
%
% Esempi:
%   deviation  -> gemelli (stream mask_range + mask_dephase) in UN
%                      solo YAML; UN solo \lstinputlisting che copre i due
%                      stream (linerange da verificare sul file reale);
%                      figura a due pannelli deviation_map.
%   ex1_dephase     -> quarto angolo: dephase globale [[0,0],[1,100]] SENZA
%                      alcun range -> jitter implicito (micromodulazione).
%                      Mostrato inline (una sola chiave); figura opzionale.
%   exA_micro       -> controparte UDIBILE (freeze dry/wet) nel bundle.
%
% Pattern: aggancio all'identità di contenuto di §2.3 -> gemelli (la sola
%   posizione dell'envelope, offset_range come istanza) -> lettura figura
%   -> meccanismo: due equazioni a confronto (Truax eq:truax vs gated
%   eq:gated) -> quadrato 2x2 (envelope puro / Truax / gated / quarto
%   angolo = jitter implicito) con tabella dei jitter -> controparte sul
%   contenuto della distribution di §2.3 -> riproducibilità -> ponte §2.5
%
% Dipendenze di preambolo:
%   - amsmath  (equation, \eqref, \tfrac)
%   - textcomp (\textdegree)  [già usato altrove]
%   - capt-of/caption (\captionof)  [già usato per le figure]
%   - tabella: \hline (niente booktabs); se booktabs è disponibile,
%     \toprule/\midrule/\bottomrule rendono meglio.
%
% TODO prima della submission:
%   [ ] linerange del listato unico: verificare i numeri sul file reale
%       (deve coprire entrambi gli stream, mask_range + mask_dephase)
%   [ ] \ref del rinvio di generalità (parametri con range+dephase):
%       allineare al label reale della sezione che li dispiega (sec:voci?)
%   [x] tabella jitter: ora generata da examples/gen_jitter_table.py dai
%       default_jitter del PGE pinnato (make jitter-table). Verificato:
%       volume/pan/durata/lettura = ±default_jitter/2 (UniformDistribution);
%       pitch ±12 cents = EDO_IMPLICIT_DETUNE_CENTS (semi-ampiezza diretta);
%       footnote ratio corretta a ±0,0025 (era ±0,005, errata di un fattore 2).
%   [ ] \cite{Truax1988}, \cite{Vaggione2002}: chiavi reali
%   [ ] \ref{fig:pointer-MAP}, \ref{sec:griglia}, \ref{sec:pointer},
%       \ref{sec:voci}: verificare label
%   [ ] figura deviation_map a colonna singola: leggibilità a 8,2 cm
%       (due pannelli impilati); valutare figure* se troppo piccola
%   [ ] (opzionale) figura del log di ex1_dephase (banda -> nuvola)
% =====================================================================

% ----------------------------------------------------------------
\subsection{La deviazione per grano: ampiezza e probabilità}\label{sec:deviazione}

A lettura ferma i grani restano identici nel contenuto, e con la
\texttt{distribution} (\S\ref{sec:griglia}) ne abbiamo irregolarizzato
soltanto il tempo. Sciogliere anche l'identità di contenuto è la
deviazione per grano: attorno alla traiettoria dichiarata, ogni
parametro può ricevere, grano per grano, uno scarto campionato in modo
indipendente. Il sistema fattorizza questa stocasticità in due
dimensioni a loro volta indipendenti --- \emph{quanto} un grano può
deviare, e \emph{con quale probabilità} la deviazione avviene --- ed
entrambe accettano envelope. I due gemelli di questa sottosezione vivono
in un solo YAML, due stream affiancati in un'unica map, costruiti per
isolarle.

\begin{figure}[t]
    \includegraphics[width=\linewidth]{examples/deviation/deviation_map}\\
    \captionof{figure}{I due gemelli della deviazione in un'unica map
    (esempio \texttt{ex3\_deviazione}, due stream; assi come in
    Fig.~\ref{fig:pointer-MAP}). Pannello superiore (\texttt{mask\_range}):
    cresce l'\emph{ampiezza} della deviazione --- cuneo a riempimento
    uniforme, ogni grano devia. Pannello inferiore (\texttt{mask\_dephase}):
    a banda massima invariata cresce la \emph{probabilità} --- la linea
    centrale persiste mentre la popolazione deviante si infittisce. Stessa
    base, stessa banda massima: differiscono per la sola posizione
    dell'envelope.}
    \label{fig:gemelli-deviazione}
\end{figure}

I due gemelli condividono la base --- lettura congelata su un punto del
buffer, come al termine di \S\ref{sec:pointer} --- e la stessa banda
massima di deviazione; differiscono per la sola posizione dell'envelope
(Listato~\ref{lst:gemelli}). Nel primo (\texttt{mask\_range}) l'envelope
sta sull'\emph{ampiezza}: il range di deviazione del puntatore di lettura
cresce da zero al 35\% del buffer e, in assenza di \texttt{dephase}, la
maschera è sempre attiva, ogni grano devia. Nel secondo
(\texttt{mask\_dephase}) l'ampiezza resta fissa al massimo del primo e a
crescere da 0 a 100\% è la chiave \texttt{dephase}, la frazione di grani a
cui la deviazione si applica. La deviazione è qui esposta sulla posizione
di lettura (\texttt{offset\_range}, \texttt{dephase.pointer}), scelta come
istanza: il dispositivo --- traiettoria centrale, range, gate --- è
definito identicamente per ogni parametro che ammetta un range e un
\texttt{dephase} (volume, pan, durata, pitch), come gli esempi successivi
dispiegano (\S\ref{sec:voci}).

    \lstinputlisting[
      linerange={49-70},
      language=yaml,
      basicstyle=\ttfamily\fontsize{7.5}{9}\selectfont,
      label=lst:gemelli,
      caption={I due gemelli in un solo YAML (\texttt{ex3\_deviazione.yml}):
      base condivisa (\texttt{freeze} a 0.5, \texttt{speed\_ratio} nullo) e
      stessa banda massima; \texttt{mask\_range} pone l'envelope
      sull'ampiezza (\texttt{offset\_range}), \texttt{mask\_dephase} sulla
      probabilità (\texttt{dephase.pointer}).}
    ]{examples/deviation/deviation.yml}

La Fig.~\ref{fig:gemelli-deviazione} affianca le due map, e le due
morfologie non potrebbero essere più diverse. Nel pannello superiore un
cuneo: la banda si allarga con continuità e i grani la riempiono
uniformemente, nessuno resta sulla linea di partenza perché tutti deviano
--- di poco all'inizio, di molto alla fine. Nel pannello inferiore la
linea centrale persiste per l'intera durata, mentre una popolazione
crescente la abbandona saltando da subito sull'intera banda: non un
allargamento ma una mistura, grani fedeli e grani devianti in proporzione
componibile nel tempo. Stessa base, stessa banda massima: ampiezza e
probabilità della deviazione sono assi indipendenti del controllo.

Il meccanismo sottostante è la \emph{tendency mask} della tradizione
granulare~\cite{Truax1988}. Per ogni parametro essa assegna al grano $n$,
di onset normalizzato $\tau_n$, una traiettoria centrale $c$ e un range di
deviazione $\rho$ --- entrambi envelope --- con un campionamento
indipendente per grano:
\begin{equation}
  v_n \;=\; c(\tau_n) \;+\; \xi_n\,\rho(\tau_n),
  \qquad \xi_n \sim \mathcal{U}\!\left(-\tfrac{1}{2},\,\tfrac{1}{2}\right),
  \label{eq:truax}
\end{equation}
dove $\xi_n$ è il campione per grano (in alternativa una distribuzione
gaussiana, in cui $\rho$ funge da deviazione standard). Il gemello A è
questo modello all'opera. Il contributo proprio del sistema è un secondo
fattore, anch'esso componibile come envelope: un gate di probabilità $g_n$
che decide grano per grano \emph{se} la maschera si applica,
\begin{equation}
  v_n \;=\; c(\tau_n) \;+\; g_n\,\xi_n\,\rho(\tau_n),
  \qquad g_n \sim \mathrm{Bernoulli}\!\left(p(\tau_n)\right),
  \label{eq:gated}
\end{equation}
con $p$ la chiave \texttt{dephase}. La sola differenza fra le due è il
fattore $g_n$: per $p\equiv 1$ si ha $g_n\equiv 1$ e la~\eqref{eq:gated}
ricade nella~\eqref{eq:truax}. Ampiezza ($\rho$) e probabilità ($p$) sono
dunque due envelope ortogonali --- il gemello A muove la prima a gate
aperto, il gemello B la seconda a banda fissa.

Il quadro delle combinazioni si lascia ora leggere per intero. Senza
range né \texttt{dephase}, il parametro segue l'envelope puro: è il regime
interamente scritto di \S\ref{sec:pointer}. Range senza \texttt{dephase}:
la maschera sempre attiva di Truax, la~\eqref{eq:truax} (gemello A). Range
con \texttt{dephase}: la maschera \emph{gated}, la~\eqref{eq:gated}
(gemello B). Resta l'ultimo angolo, il \texttt{dephase} senza alcun range
--- la~\eqref{eq:gated} con $\rho$ posto al default di sistema $\rho_0$.
Basta allora la sola chiave globale \texttt{dephase:\ [[0,0],[1,100]]}
sulla base congelata (esempio \texttt{ex1\_dephase}): il gate apre, per
ogni parametro privo di range esplicito, deviazioni implicite di entità
minima (Tab.~\ref{tab:jitter}), e il log passa con continuità da banda
netta a nuvola. È una micromodulazione che non disegna alcuna traiettoria
ma decorrela la massa grano per grano, nei termini della
\emph{décorrélation microtemporelle} di Vaggione~\cite{Vaggione2002}, ed è
la controparte, sull'asse del contenuto, di ciò che la \texttt{distribution}
opera sul tempo (\S\ref{sec:griglia}): infatti, irregolarizzando ciò che
ciascun grano legge, disperde la stessa periodicità che il congelamento
imponeva (la sua resa udibile, \texttt{freeze} dry/wet, è
\texttt{exA\_micro} nel bundle).

\begin{table}[h]
  \centering
  \captionof{table}{Jitter impliciti per parametro: deviazione massima per
  grano sotto campionamento uniforme ($\pm\rho_0/2$), applicata dal gate in
  assenza di range esplicito.}
  \label{tab:jitter}
  % Corpo (tabular) e footnote generati da examples/gen_jitter_table.py
  % (make jitter-table) dai default_jitter del PGE pinnato: i numeri non
  % possono divergere dal codice citato. Non editarli a mano.
  \input{examples/jitter_table}
\end{table}

Una parola sulla riproducibilità, qui che la stocasticità è in primo
piano: due rendering della stessa specifica producono grani diversi ma la
stessa morfologia. Ciò che lo YAML determina --- ed è lo YAML che il
bundle spedisce --- è l'andamento; la realizzazione allegata ne è
un'istanza.

Tutto ciò che precede riguarda però ancora una voce sola: un unico
puntatore di lettura, un'unica griglia di emissione, una massa di grani
indifferenziata. Lo scostamento successivo moltiplica la voce.