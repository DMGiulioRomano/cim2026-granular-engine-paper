% =====================================================================
% Cappello di sezione + sec:c-e (integrata in paper.tex via \input)
%
% Labels definiti qui e usati nelle sottosezioni
% (ordine = sequenza degli \input, NON numeri fissi):
%   sec:c-e        Lo stream minimo            (identity)
%   sec:pointer    La posizione di lettura     (pointer)
%   sec:griglia    La griglia temporale        (distribution)
%   sec:deviazione Deviazione: ampiezza e probabilità (deviation, probability)
%   —              L'esempio di mezzo          (duration)
%   —              L'esempio completo          (complete_example)
%   sec:voci       La molteplicità delle voci  (voices)
%
% TODO prima della submission:
%   [x] link audio anonimo (OSF view-only) nella footnote del cappello
%   [x] i path examples/ esistono dopo la rinomina: verificati sul .fls
%       della compilazione. Nomi = token semantico della cartella, senza
%       numeri d'ordine (convenzione in paper/examples/README.md)
% =====================================================================

% --- note di questa sezione (definite qui, richiamate nel corpo) ---
\newcommand{\notaRepo}{\footnote{La libreria Python (compatibilità da 3.9 a 3.12)
è manutenuta presso un repository
github METTERE LINK. È in tempo differito.
Dipendenze principali NumPy e \texttt{soundfile} per rendering audio,
matplotlib per la \textsc{map}. Il costo di calcolo non cresce con la durata d'ascolto ma col numero di
grani. Le realizzazioni audio degli esempi di questo paper
sono disponibili in un archivio ad accesso aperto:
\emph{https://osf.io/xqmh5/?view\_only=5a6d9ac26adb466697477801e73b153f}.}}

\newcommand{\notaGit}{\footnote{\emph{git} è un sistema di controllo di versione
distribuito: registra la storia completa delle modifiche a un insieme di file
testuali, permette di confrontare due stati (\emph{diff}) e di tornare a
qualsiasi punto precedente. Software libero, documentazione e distribuzione
su \emph{https://git-scm.com}.}}

\newcommand{\notaGamma}{\footnote{Prima esecuzione assoluta il 10 luglio 2025
al Museo degli Strumenti Musicali di Roma, nel concerto \emph{INCONTRI} del Festival ArteScienza 2025.}}

% ================================================================
\section{DIRAC: un'architettura per l'indagine parametrica}\label{sec:architettura}
PythonGranularEngine\notaRepo\ è un motore
di sintesi per la composizione di \emph{fixed media} attraverso granulazione.
L'ho scritto con un fine pedagogico per chiunque voglia acquisire consapevolezza
sulla tecnica come strumento di pensiero compositivo. La scelta del tempo differito è deliberata: si oppone a una prassi
consolidata, che Roads descrive come una composizione per brevi
improvvisazioni in ambienti real-time, dove si lavora mentre
«\emph{the tape is running}» e le parti riuscite si selezionano in un
secondo momento~\cite[p.~32]{Roads2021}. L'interfaccia in questi ambienti
è spesso un controller~\cite[pp.~193--194]{Roads2001}, per colmare il gap
del rapporto con lo strumento e con la sua gestualità.
Poiché questa libreria è invece pensata per un approccio alternativo a questo,
l'input è un linguaggio di dominio testuale in YAML che espone
un'interfaccia dichiarativa. La scelta è il risultato di un processo di ricerca personale
di circa due anni attraverso altre esperienze come GAMMA\notaGamma, brano sistemico 
con un altro motore di composizione assistita per sintesi additiva.
I motivi maggiori sono due. Il versionamento — \emph{git}\notaGit\ come documentazione del processo, con la riproducibilità e la sostenibilità che 
ne seguono — e la possibilità di lavorare in dialogo con un modello linguistico che può fare facilmente inferenza sull'input e divenendo una potenziale
altra interfaccia. Il secondo è la possibilità di un dominio semantico espandibile 
e modificabile senza modificare la libreria.

In questo capitolo porterò una serie di esempi a partire da una 
semplice riproduzione del materiale audio scelto, modificando 
pochi parametri alla volta e mostrandone il listato.
Ogni esempio che segue è accompagnato dalla sua \textsc{MAP} 
(\emph{Multiparametric Audio Plot}), una mappa sinottica
come in Fig.~\ref{fig:pointer-MAP}.
In ascissa scorre il tempo del brano, nelle ordinate la posizione di
lettura nel buffer visibile a sinistra attraverso la sua forma 
d'onda. Ogni grano è un poligono composto di corpo e testa: la posizione
indica verticalmente il punto di lettura e orizzontalmente l'\emph{on-set}.
l'altezza e la larghezza sono la sua durata rispettivamente nel buffer e nel tempo.
L'orientamento della testa indica il verso di lettura del buffer.
Nella testa può essere disegnato il profilo della
finestra applicata al tale grano, come in Fig.~\ref{fig:completo}. Il
colore codifica la differenza di altezza (secondo la scala scelta) rispetto al materiale originale 
ed è ispezionabile confrontando la \emph{colormap} a destra come legenda.
L'etichetta stampata sulla traccia è lo \texttt{stream\_id}~\ref{sec:c-e} dichiarato. 
Il pannello subito sotto riporta gli inviluppi dichiarati,
con i breakpoint annotati ciascuno col proprio valore.

Questo prodotto grafico del motore è fondamentale per avere consapevolezza
e comprensione della specifica YAML dichiarata in input potendo osservare
a colpo d'occhio il comportamento di uno stream. Per economia di mezzi e spazio 
userò quasi sempre un solo stream per volta per non stressare la grandezza della figura.
In esempi piu fitti con molti parametri e streams attivi è divenuto
necessario nel mio workflow nello stato di test o debugging e 
per comprendere come agire puntualmente su una 
specifica popolazione di grani.
Prima degli esempi conviene dichiarare una volta la forma ricorrente del
linguaggio, invece di lasciarla ricostruire un esempio alla volta. Quasi ogni
parametro si scrive su tre chiavi indipendenti: il valore base (\texttt{pan}),
la banda entro cui il valore può scostarsene grano per grano
(\texttt{pan\_range}), e la probabilità che quello scostamento si applichi
(\texttt{deviation\_probability.pan}). Le tre si combinano liberamente. Una
banda dichiarata senza probabilità si applica a tutti i grani; una probabilità
dichiarata senza banda apre su una deviazione implicita, propria di ciascun
parametro. E ciascuna delle tre accetta uno scalare o un inviluppo a
breakpoint, quindi ognuna può variare lungo il brano per conto suo: è su questa
indipendenza che lavorano gli esempi da~\ref{sec:deviazione} in avanti. Il
Listato~\ref{lst:grammatica} riporta l'intera grammatica in questa forma — è la
specifica completa del linguaggio, non la sola porzione che gli esempi
esercitano.

% L'albero è generato da figures/gen_grammar_tree.py (make grammar-tree) dai
% registri del PGE pinnato: domini, bounds ed enum non possono divergere dal
% motore citato. firstline=4 salta le tre righe di intestazione del file.
\begin{figure}[!t]
    \begin{framedfloat}
    \lstinputlisting[
      language=yaml,
      firstline=4,
      basicstyle=\ttfamily\fontsize{7}{7.5}\selectfont,
      label=lst:grammatica,
      caption={La grammatica del linguaggio: proprietà, sottoproprietà e
      domini. Non è un esempio ma lo schema — nessun documento dichiara tutte
      queste chiavi, e fra parentesi angolari non c'è un valore ma il dominio
      ammesso. Dove il dominio è un intervallo, il valore può essere
      indifferentemente uno scalare o un inviluppo a breakpoint. Gli elenchi
      lunghi sono troncati, col numero totale fra parentesi. Il listato è
      generato dal codice della versione citata: i domini non possono divergere
      dal motore.}
    ]{figures/grammar_tree.tex}
    \end{framedfloat}
\end{figure}

A seguire le condizioni minime di esistenza di uno stream.
