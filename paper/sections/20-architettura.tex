% =====================================================================
% Cappello di sezione + sec:c-e (integrata in paper.tex via \input)
%
% Labels definiti qui e usati nelle sottosezioni
% (ordine = sequenza degli \input, NON numeri fissi):
%   sec:c-e        Lo stream minimo
%   sec:griglia    La griglia temporale        (ex1_distribution)
%   sec:pointer    La posizione di lettura     (pointer)
%   sec:deviazione Deviazione: ampiezza e probabilità (ex3a/ex3b)
%   sec:voci       La molteplicità delle voci  (ex4, ex5)
%
% TODO prima della submission:
%   [ ] DOI Zenodo anonimo nella footnote del cappello
%   [ ] verificare che i path examples/identity/... esistano dopo la rinomina
% =====================================================================

% ================================================================
\section{DIRAC: un'architettura per l'indagine parametrica}\label{sec:architettura}
Ho scritto questo sistema\footnote{Il codice e gli esempi sono in un
repository pubblico ad accesso aperto: \emph{[riferimento anonimizzato in
fase di revisione]}. È scritto in Python e rende offline: un backend NumPy
diretto o una pipeline Csound in due stadi (partitura, poi audio)
materializzano i grani; dipendenze principali NumPy e \texttt{soundfile},
matplotlib per la \textsc{map}. Il costo di calcolo cresce col numero di
grani, non con la durata d'ascolto.} per comporre e con un fine pedagogico, che ritengo
possa essere utile a chiunque voglia acquisire consapevolezza
sulla tecnica come strumento di pensiero compositivo.
A seguire le condizioni minime di esistenza di uno stream.

