# graph generation tools
pyan3>=2.6.0
py2puml>=0.11.0

# knowledge extraction (nota: pacchetto PyPI si chiama graphifyy con doppia y)
graphifyy

# PGE runtime deps (required by py2puml to import PGE modules).
# Da v7.0.0 il submodule dichiara le dipendenze in pyproject.toml e il suo
# requirements.txt e' un puntatore `-e .[dev]`: incluso via `-r` da qui, pip
# risolverebbe quel `.` sulla root di QUESTO repo, che non e' un progetto
# Python. Si installa quindi il submodule come pacchetto.
-e ./raw/PythonGranularEngine
